{
  "name": "fluctus-web",
  "version": "0.1.0",
  "private": true,
  "description": "Fluctus publieke website + app-portal (Academy-login, RBAC-launcher) + offerte-automatisering (Odoo). P1.",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "concurrently -k \"vite\" \"node --watch server/index.js\"",
    "build": "vite build",
    "start": "node server/index.js",
    "check": "node --check server/index.js && node --check server/routes/offerte.js && node --check server/routes/contact.js && node --check server/routes/webhook.js && node --check server/lib/odoo.js && node --check server/lib/supabase.js && node --check server/lib/brevo.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "vite": "^5.4.0"
  }
}
